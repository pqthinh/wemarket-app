import styled from 'styled-components'
import { Layout, Select } from '@ui-kitten/components'

export const ContainerSelect = styled(Layout)`
  display: flex;
  flex: 1;
  flex-direction: row;
  justify-content: space-between;
  height: 100%;
`
export const SelectWrapper = styled(Select)`
  flex: 1;
  margin: 2px;
`
